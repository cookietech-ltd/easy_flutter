import 'package:easy_flutter/easy_flutter.dart';

class CoreInitializer implements Initializer {
  @override
  Future<void> init() async {
    // Register core services here (e.g. storage, session, logging).
  }
}
